import styles from "./IconNotice.module.css";

const IconNotice = () => {
	return (
		<div className={styles.container}>
			<div className={styles.noticeDot} />
		</div>
	);
};

export default IconNotice;
